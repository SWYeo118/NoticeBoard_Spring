package kr.ac.kopo.ctc.spring.board.repo.mapper;

import java.util.List;
import java.util.Optional;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.session.RowBounds;
import org.springframework.stereotype.Repository;

import kr.ac.kopo.ctc.spring.board.domain.BoardItem;

@Mapper
@Repository
public interface BoardItemMapper {
//	List<BoardItem> findOne(BoardItemCondition condition);
//	List<BoardItem> findAll();
//	List<BoardItem> findByPaging(BoardItemCondition condition, RowBounds rowBounds);
//	List<BoardItem> findBySearching(BoardItemCondition condition);
//	List<BoardItem> findBySearchingAndPaging(BoardItemCondition condition, RowBounds rowBounds);
//	Optional<BoardItem> insertOne(BoardItemCondition condition);
//	Optional<BoardItem> updateOne();
//	Optional<BoardItem> deleteOne();
}
